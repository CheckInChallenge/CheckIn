user: "${{ github.event.issue.user.login }}"
          flag: "${{ secrets.FLAG }}"
        run: |
          echo "$user:$issues" >> issues
          echo "$flag:$issues" >> issues
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add .